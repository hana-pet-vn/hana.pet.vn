'use client'
// app/admin2/orders/page.js — placeholder, làm ở Phase 1
import { T } from '../_lib/tokens'

export default function Page() {
  return (
    <div>
      <h1 style={{ fontFamily: T.fontTitle, fontSize: 19, color: T.navyDeep, marginBottom: 4 }}>
        Đơn hàng
      </h1>
      <p style={{ color: T.muted }}>Trang này sẽ được xây ở Phase 1.</p>
    </div>
  )
}
